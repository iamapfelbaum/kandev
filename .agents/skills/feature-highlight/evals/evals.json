{
  "skill_name": "feature-highlight",
  "evals": [
    {
      "id": 1,
      "prompt": "Act as a fresh agent. Use `--template quick-start` to scaffold and render the canonical deterministic desktop Highlight that creates one task. Show cheap storyboard review, a zero-write dry-run, the explicit closed runtime and PR source proof, recoverable run selection, external review-stage-v2 evidence, and separate promotion. I do not want custom capture, camera, or encoder scripts.",
      "expected_output": "A schema-v1 flow where the canonical quick-start cannot override its pinned identity, stable selectors and deterministic seed/setup use the exact desktop profile, default no zoom is preserved, and `--runtime kandev-isolated-e2e` runs with `--source pr_head`, `--pr-number`, and `--pr-base-sha`. It preserves the run ID for `--run-id` recovery, stages `kandev-highlight-review-stage-v2`, reports truthful `runtimeLogs: false` and `renderedPixelOcr: false` coverage, and promotes only after explicit human acceptance.",
      "files": [
        "scripts/highlights/examples/quick-start.scenario.json",
        "apps/web/e2e/highlights/quick-start.scenario.json"
      ],
      "expectations": [
        "Uses node scripts/highlights.mjs scaffold, validate, storyboard, run, and promote with --template quick-start",
        "Uses --runtime kandev-isolated-e2e and --source pr_head with --pr-number and --pr-base-sha",
        "Preserves the automatic run ID and uses --run-id for render, QA, or stage recovery",
        "Identifies pnpm --dir apps/web e2e:highlight-pipeline as the canonical fresh-agent integration/eval",
        "Uses testId or role plus exact accessible name and no arbitrary JavaScript or shell",
        "States that default camera is centered 1x with no zoom by default",
        "Keeps raw masters and QA outside the repository",
        "Requires automatic media, containment, motion, sensitive-data, contact-sheet, browser, hash, and byte QA while declaring runtimeLogs false and renderedPixelOcr false",
        "Promotes only kandev-highlight-review-stage-v2 after accepted review and refuses an existing immutable revision"
      ]
    },
    {
      "id": 2,
      "prompt": "Author native-phone and desktop Highlight scenarios for the same board story. Zoom should stay calm, cursor motion must remain smooth, and mobile must not be a crop.",
      "expected_output": "Two native scenario routes with exact CSS/DPR/source profiles, deterministic shared seed intent, independent semantic actions, identity camera unless explicit focus is useful, bounded 1.5x desktop and 1.18x mobile motion, containment, and settled bookends.",
      "files": [],
      "expectations": [
        "Uses 1920x1200 DPR2 to 3840x2400 desktop source and 1920x1200 delivery at 25 fps",
        "Uses 430x932 DPR3 native-mobile context and 1290x2796 source/delivery at 25 fps",
        "Never crops desktop into mobile",
        "Rejects hover and middle/right click on native mobile and uses trusted touch tap/drag",
        "Separates cursor control from focus/zoom/hold/return camera directives",
        "Requires camera motion, safe-margin, target-glyph, cursor, and opening/ending settle QA"
      ]
    },
    {
      "id": 3,
      "prompt": "Migrate an accepted old pilot that has a hand-written Playwright script, camera JSON, encoder script, contact sheet, and manual asset copy. Only poster and framing need to change.",
      "expected_output": "A migration map from bespoke artifacts to scenario actions, registered seed/setup, shared landing adapter, automatic QA, external stage, and immutable promotion; it reuses the clean raw when provenance/source digest and semantic evidence pass instead of recapturing solely for migration.",
      "files": [],
      "expectations": [
        "Does not recapture merely for migration",
        "Reuses clean raw for framing/poster/pacing only when source digest and QA permit",
        "Maps hand-authored camera and encoder code to scripts/product-loop-highlight.mjs",
        "Preserves prior accepted revision and promotes a new immutable revision without overwrite"
      ]
    }
  ]
}
